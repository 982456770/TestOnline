<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.testonline">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".teacher.StuAnswerActivity"></activity>
        <activity android:name=".teacher.ShowQActivity" />
        <activity android:name=".teacher.DelQActivity" />
        <activity android:name=".teacher.InsQActivity" />
        <activity android:name=".student.MathActivity" />
        <activity android:name=".student.StudentActivity" />
        <activity android:name=".teacher.TeacherActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>